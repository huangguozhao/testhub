# TestHub Java 版本架构草案

## 文档信息

| 属性 | 内容 |
|------|------|
| 产品名称 | TestHub 智能测试管理平台 |
| 版本 | Java 版 v1.0 |
| 文档状态 | 架构草案（可迭代） |
| 创建日期 | 2026-04-13 |
| 最后更新 | 2026-04-13 |

> **注意**：本文档为架构草案，允许持续迭代。每次迭代需在「架构迭代记录」章节记录修改原因。
>
> **正式架构文档**：`docs/java-version/03-technical/03-架构文档.md`

---

## 一、架构原则

> **轻量化、可迭代、不固定死规**

1. **简洁分层**：避免过度工程化，先满足业务需求
2. **模块自治**：每个模块独立可测试，降低耦合
3. **渐进增强**：先实现核心功能，后续迭代优化
4. **文档跟随**：架构变更必须同步更新文档，记录原因

---

## 二、目录分层

### 2.1 项目结构总览

```
testhub-java/
├── pom.xml                          # Maven 父 POM
├── src/
│   ├── main/
│   │   ├── java/com/testhub/
│   │   │   ├── TesthubApplication.java    # 启动类
│   │   │   │
│   │   │   ├── config/                    # 配置层
│   │   │   │   ├── SecurityConfig.java    # Spring Security 配置
│   │   │   │   ├── WebConfig.java         # Web 配置（CORS、拦截器）
│   │   │   │   ├── RedisConfig.java       # Redis 配置
│   │   │   │   ├── SwaggerConfig.java     # OpenAPI 文档配置
│   │   │   │   └── XXLJobConfig.java      # 任务调度配置
│   │   │   │
│   │   │   ├── common/                    # 公共模块
│   │   │   │   ├── result/                # 统一响应结构
│   │   │   │   │   ├── Result.java        # 统一响应包装
│   │   │   │   │   ├── ResultCode.java    # 响应码枚举
│   │   │   │   │   └── PageResult.java    # 分页响应
│   │   │   │   ├── exception/              # 异常处理
│   │   │   │   │   ├── GlobalExceptionHandler.java
│   │   │   │   │   ├── BusinessException.java
│   │   │   │   │   └── UnauthorizedException.java
│   │   │   │   ├── utils/                  # 工具类
│   │   │   │   │   ├── JwtUtils.java      # JWT 工具
│   │   │   │   │   ├── PasswordEncoder.java
│   │   │   │   │   ├── SnowflakeIdGenerator.java
│   │   │   │   │   └── DateUtils.java
│   │   │   │   └── constant/               # 常量定义
│   │   │   │       └── SystemConstants.java
│   │   │   │
│   │   │   ├── module/                    # 业务模块（核心）
│   │   │   │   ├── user/                  # 用户模块
│   │   │   │   │   ├── controller/
│   │   │   │   │   │   └── UserController.java
│   │   │   │   │   ├── service/
│   │   │   │   │   │   ├── UserService.java
│   │   │   │   │   │   └── impl/
│   │   │   │   │   │       └── UserServiceImpl.java
│   │   │   │   │   ├── mapper/
│   │   │   │   │   │   └── UserMapper.java
│   │   │   │   │   ├── entity/
│   │   │   │   │   │   ├── User.java
│   │   │   │   │   │   └── UserProfile.java
│   │   │   │   │   ├── dto/
│   │   │   │   │   │   ├── LoginRequest.java
│   │   │   │   │   │   ├── RegisterRequest.java
│   │   │   │   │   │   └── UserVO.java
│   │   │   │   │   └── auth/
│   │   │   │   │       ├── AuthController.java
│   │   │   │   │       ├── AuthService.java
│   │   │   │   │       ├── JwtAuthFilter.java
│   │   │   │   │       └── TokenService.java
│   │   │   │   │
│   │   │   │   ├── project/               # 项目模块
│   │   │   │   ├── testcase/              # 手工测试用例模块
│   │   │   │   ├── testsuite/             # 测试套件模块
│   │   │   │   ├── execution/             # 测试执行模块
│   │   │   │   ├── report/                # 测试报告模块
│   │   │   │   ├── review/                # 测试评审模块
│   │   │   │   ├── version/               # 版本管理模块
│   │   │   │   ├── api-testing/           # API 测试模块
│   │   │   │   ├── ui-automation/         # UI 自动化模块
│   │   │   │   ├── app-automation/        # APP 自动化模块
│   │   │   │   ├── requirement-analysis/  # AI 需求分析模块
│   │   │   │   └── assistant/             # AI 助手模块
│   │   │   │
│   │   │   └── job/                       # 定时任务模块
│   │   │       ├── JobController.java     # XXL-JOB 任务注册
│   │   │       └── tasks/                 # 具体任务实现
│   │   │           ├── ApiScheduledTask.java
│   │   │           └── UiScheduledTask.java
│   │   │
│   │   └── resources/
│   │       ├── application.yml            # 主配置文件
│   │       ├── application-dev.yml        # 开发环境
│   │       ├── application-prod.yml       # 生产环境
│   │       ├── mapper/                    # MyBatis XML 映射文件
│   │       │   ├── user/
│   │       │   │   └── UserMapper.xml
│   │       │   └── project/
│   │       │       └── ProjectMapper.xml
│   │       └── templates/                  # 代码生成模板（可选）
│   │
│   └── test/
│       └── java/com/testhub/              # 单元测试
│           ├── module/
│           │   └── user/
│           │       └── UserServiceTest.java
│           └── common/
│               └── JwtUtilsTest.java
│
├── docs/                                  # 架构文档
├── scripts/                               # 部署脚本
└── docker/                                # Docker 配置
```

### 2.2 分层说明

| 层级 | 目录 | 职责 | 说明 |
|------|------|------|------|
| **配置层** | config/ | 应用配置、Bean 注册 | 集中管理配置类 |
| **公共层** | common/ | 通用工具、统一响应、异常处理 | 被所有模块复用 |
| **业务层** | module/ | 核心业务逻辑 | 按功能模块划分 |
| **任务层** | job/ | 定时任务实现 | 依赖业务层 |

### 2.3 模块内部结构

每个业务模块遵循统一结构：

```
module/
├── controller/        # 控制层：接收请求、参数校验、调用 service
├── service/           # 服务层：业务逻辑、事务管理
│   └── impl/         # 服务实现
├── mapper/           # 数据访问层：MyBatis Mapper 接口
├── entity/           # 实体层：数据库表映射
├── dto/              # 数据传输对象：请求/响应 DTO
├── vo/               # 视图对象：返回给前端的结构
└── enums/            # 枚举类
```

---

## 三、核心模块设计

### 3.1 模块划分总览

| # | 模块名称 | 英文名 | 核心职责 | 依赖模块 |
|---|----------|--------|----------|----------|
| 1 | 用户认证 | user | 用户注册、登录、Token 管理 | 无 |
| 2 | 项目管理 | project | 项目 CRUD、成员管理、环境配置 | user |
| 3 | 手工测试用例 | testcase | 用例 CRUD、步骤管理、附件、评论 | project |
| 4 | 测试套件 | testsuite | 套件管理、用例关联 | testcase |
| 5 | 测试执行 | execution | 测试计划、执行记录 | testsuite, testcase |
| 6 | 测试报告 | report | 报告生成、导出 | execution |
| 7 | 测试评审 | review | 评审流程、评审模板 | testcase |
| 8 | 版本管理 | version | 版本 CRUD、版本对比 | project |
| 9 | API 测试 | api-testing | API 请求、集合、环境、定时任务 | project |
| 10 | UI 自动化 | ui-automation | 元素管理、脚本执行、定时任务 | project |
| 11 | APP 自动化 | app-automation | 设备管理、组件管理、执行 | project |
| 12 | AI 需求分析 | requirement-analysis | 文档解析、用例生成、评审 | project, LangChain4j |
| 13 | AI 助手 | assistant | Dify 集成、对话管理 | user |
| 14 | 测试数据工厂 | data-factory | 测试数据生成 | 无 |
| 15 | 统一通知 | notification | 邮件、Webhook 通知 | 无 |

### 3.2 模块依赖关系图

```
                    ┌─────────────┐
                    │   common    │
                    └──────┬──────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
         ▼                 ▼                 ▼
    ┌─────────┐       ┌─────────┐       ┌─────────────┐
    │   user  │       │project  │       │data-factory │
    └────┬────┘       └────┬────┘       └─────────────┘
         │                 │
         └────────┬────────┘
                  │
    ┌─────────────┼─────────────┬─────────────┐
    │             │             │             │
    ▼             ▼             ▼             ▼
┌────────┐  ┌──────────┐  ┌───────────┐  ┌────────────┐
│testcase│  │api-testing│  │ui-automation│  │app-automation│
└───┬────┘  └─────┬────┘  └──────┬─────┘  └───────┬──────┘
    │              │              │                │
    ▼              ▼              ▼                ▼
┌────────┐  ┌──────────┐  ┌───────────┐  ┌────────────┐
│testsuite│  │  job     │  │   job      │  │   job       │
└───┬────┘  └──────────┘  └───────────┘  └────────────┘
    │
    ▼
┌──────────┐     ┌───────────┐     ┌────────────┐
│execution │────►│  report   │     │  review     │
└──────────┘     └───────────┘     └────────────┘

┌─────────────────────────────────────────────┐
│          requirement-analysis                │
│         (依赖 project + LangChain4j)        │
└─────────────────────────────────────────────┘
```

### 3.3 核心模块详细设计

#### 3.3.1 用户认证模块 (user)

**职责**：用户注册、登录、Token 刷新、退出

**关键类**：

| 类名 | 类型 | 职责 |
|------|------|------|
| UserController | Controller | 用户信息 CRUD |
| AuthController | Controller | 登录/注册/Token |
| UserService | Service | 用户业务逻辑 |
| AuthService | Service | 认证业务逻辑 |
| JwtAuthFilter | Filter | JWT 认证过滤器 |
| TokenService | Service | Token 管理（生成、刷新、验证） |
| User | Entity | 用户实体 |
| UserProfile | Entity | 用户配置实体 |

**API 设计**：

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录 |
| POST | /api/auth/logout | 退出登录 |
| POST | /api/auth/token/refresh | 刷新 Token |
| GET | /api/users/me | 获取当前用户信息 |
| PUT | /api/users/me | 更新当前用户信息 |

#### 3.3.2 项目管理模块 (project)

**职责**：项目 CRUD、成员管理、环境变量

**关键类**：

| 类名 | 类型 | 职责 |
|------|------|------|
| ProjectController | Controller | 项目 CRUD |
| ProjectMemberController | Controller | 成员管理 |
| EnvironmentController | Controller | 环境管理 |
| ProjectService | Service | 项目业务逻辑 |
| ProjectMemberService | Service | 成员业务逻辑 |
| Project | Entity | 项目实体 |
| ProjectMember | Entity | 项目成员实体 |
| ProjectEnvironment | Entity | 项目环境实体 |

#### 3.3.3 AI 需求分析模块 (requirement-analysis)

**职责**：文档解析、AI 生成测试用例、AI 评审

**关键类**：

| 类名 | 类型 | 职责 |
|------|------|------|
| RequirementDocumentController | Controller | 文档管理 |
| AiServiceController | Controller | AI 服务调用 |
| RequirementDocumentService | Service | 文档解析、文本提取 |
| TestCaseGeneratorService | Service | 测试用例生成 |
| TestCaseReviewerService | Service | 测试用例评审 |
| AiModelConfigService | Service | AI 模型配置管理 |
| AiModelClient | Client | AI 模型 HTTP 客户端（封装 LangChain4j） |

---

## 四、数据模型设计

### 4.1 核心实体关系

```
User (用户)
  │
  ├── 1:1 ─── UserProfile (用户配置)
  │
  └── N:M ─── Project (项目) ─── ProjectMember (项目成员)
                      │
                      ├── 1:N ─── TestCase (手工测试用例)
                      │           │
                      │           ├── 1:N ─── TestCaseStep (用例步骤)
                      │           ├── 1:N ─── TestCaseAttachment (附件)
                      │           └── 1:N ─── TestCaseComment (评论)
                      │
                      ├── 1:N ─── TestSuite (测试套件)
                      │           │
                      │           └── N:M ─── TestCase (多对多)
                      │
                      ├── 1:N ─── TestPlan (测试计划)
                      │           │
                      │           └── 1:N ─── TestRun (测试执行)
                      │
                      ├── 1:N ─── ProjectEnvironment (项目环境)
                      │
                      ├── 1:N ─── Version (版本)
                      │
                      ├── 1:N ─── RequirementDocument (需求文档)
                      │           │
                      │           └── 1:1 ─── RequirementAnalysis (需求分析)
                      │                       │
                      │                       └── 1:N ─── BusinessRequirement (业务需求)
                      │                                   │
                      │                                   └── 1:N ─── GeneratedTestCase (生成的用例)
                      │
                      ├── 1:N ─── ApiProject (API 测试项目)
                      │           │
                      │           ├── 1:N ─── ApiCollection (API 集合)
                      │           │           │
                      │           │           └── 1:N ─── ApiRequest (API 请求)
                      │           │
                      │           ├── 1:N ─── ApiEnvironment (API 环境)
                      │           │
                      │           └── 1:N ─── ApiScheduledTask (定时任务)
                      │
                      ├── 1:N ─── UiProject (UI 自动化项目)
                      │           │
                      │           ├── 1:N ─── UiPageObject (页面对象)
                      │           │           │
                      │           │           └── 1:N ─── UiElement (页面元素)
                      │           │
                      │           ├── 1:N ─── UiTestScript (测试脚本)
                      │           │
                      │           └── 1:N ─── UiScheduledTask (定时任务)
                      │
                      └── 1:N ─── AppProject (APP 自动化项目)
                                  │
                                  ├── 1:N ─── AppDevice (设备)
                                  ├── 1:N ─── AppComponent (组件)
                                  └── 1:N ─── AppScheduledTask (定时任务)
```

### 4.2 核心表结构

#### 4.2.1 用户相关表

```sql
-- 用户表
CREATE TABLE `sys_user` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `username` VARCHAR(150) NOT NULL UNIQUE COMMENT '用户名',
    `password` VARCHAR(128) NOT NULL COMMENT '密码',
    `email` VARCHAR(254) UNIQUE COMMENT '邮箱',
    `phone` VARCHAR(11) COMMENT '手机号',
    `avatar` VARCHAR(255) COMMENT '头像URL',
    `department` VARCHAR(100) COMMENT '部门',
    `position` VARCHAR(100) COMMENT '职位',
    `is_superuser` BOOLEAN DEFAULT FALSE COMMENT '超级管理员',
    `is_active` BOOLEAN DEFAULT TRUE COMMENT '是否激活',
    `last_login` DATETIME COMMENT '最后登录时间',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_username` (`username`),
    INDEX `idx_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 用户配置表
CREATE TABLE `sys_user_profile` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL UNIQUE COMMENT '用户ID',
    `theme` VARCHAR(20) DEFAULT 'light' COMMENT '主题',
    `language` VARCHAR(10) DEFAULT 'zh-cn' COMMENT '语言',
    `timezone` VARCHAR(50) DEFAULT 'Asia/Shanghai' COMMENT '时区',
    `notifications` JSON COMMENT '通知设置',
    FOREIGN KEY (`user_id`) REFERENCES `sys_user`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户配置表';

-- Refresh Token 黑名单表
CREATE TABLE `sys_token_blacklist` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `token` VARCHAR(500) NOT NULL UNIQUE COMMENT 'Token',
    `expired_at` DATETIME NOT NULL COMMENT '过期时间',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_token` (`token`),
    INDEX `idx_expired_at` (`expired_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Token 黑名单表';
```

#### 4.2.2 项目相关表

```sql
-- 项目表
CREATE TABLE `prj_project` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `name` VARCHAR(200) NOT NULL COMMENT '项目名称',
    `description` TEXT COMMENT '项目描述',
    `status` VARCHAR(20) DEFAULT 'active' COMMENT '状态',
    `owner_id` BIGINT NOT NULL COMMENT '负责人ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (`owner_id`) REFERENCES `sys_user`(`id`),
    INDEX `idx_status` (`status`),
    INDEX `idx_owner` (`owner_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='项目表';

-- 项目成员表
CREATE TABLE `prj_project_member` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `project_id` BIGINT NOT NULL COMMENT '项目ID',
    `user_id` BIGINT NOT NULL COMMENT '用户ID',
    `role` VARCHAR(20) DEFAULT 'tester' COMMENT '角色',
    `joined_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`project_id`) REFERENCES `prj_project`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`user_id`) REFERENCES `sys_user`(`id`),
    UNIQUE KEY `uk_project_user` (`project_id`, `user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='项目成员表';

-- 项目环境表
CREATE TABLE `prj_project_environment` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `project_id` BIGINT NOT NULL COMMENT '项目ID',
    `name` VARCHAR(100) NOT NULL COMMENT '环境名称',
    `base_url` VARCHAR(200) NOT NULL COMMENT '基础URL',
    `description` TEXT COMMENT '环境描述',
    `variables` JSON COMMENT '环境变量',
    `is_default` BOOLEAN DEFAULT FALSE COMMENT '是否默认',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`project_id`) REFERENCES `prj_project`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='项目环境表';
```

#### 4.2.3 测试用例相关表

```sql
-- 测试用例表
CREATE TABLE `tc_test_case` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `project_id` BIGINT NOT NULL COMMENT '项目ID',
    `title` VARCHAR(500) NOT NULL COMMENT '用例标题',
    `description` TEXT COMMENT '用例描述',
    `preconditions` TEXT COMMENT '前置条件',
    `priority` VARCHAR(20) DEFAULT 'medium' COMMENT '优先级',
    `status` VARCHAR(20) DEFAULT 'draft' COMMENT '状态',
    `test_type` VARCHAR(20) DEFAULT 'functional' COMMENT '测试类型',
    `tags` JSON COMMENT '标签',
    `author_id` BIGINT NOT NULL COMMENT '作者ID',
    `assignee_id` BIGINT COMMENT '指派人ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (`project_id`) REFERENCES `prj_project`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`author_id`) REFERENCES `sys_user`(`id`),
    FOREIGN KEY (`assignee_id`) REFERENCES `sys_user`(`id`),
    INDEX `idx_project` (`project_id`),
    INDEX `idx_author` (`author_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='测试用例表';

-- 测试用例步骤表
CREATE TABLE `tc_test_case_step` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `testcase_id` BIGINT NOT NULL COMMENT '用例ID',
    `step_number` INT NOT NULL COMMENT '步骤序号',
    `action` TEXT NOT NULL COMMENT '操作描述',
    `expected` TEXT NOT NULL COMMENT '预期结果',
    FOREIGN KEY (`testcase_id`) REFERENCES `tc_test_case`(`id`) ON DELETE CASCADE,
    UNIQUE KEY `uk_testcase_step` (`testcase_id`, `step_number`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='测试用例步骤表';
```

### 4.3 表命名规范

| 前缀 | 含义 | 示例 |
|------|------|------|
| `sys_` | 系统表 | sys_user, sys_user_profile |
| `prj_` | 项目表 | prj_project, prj_project_member |
| `tc_` | 测试用例表 | tc_test_case, tc_test_case_step |
| `ts_` | 测试套件表 | ts_test_suite, ts_suite_case |
| `exec_` | 执行记录表 | exec_test_plan, exec_test_run |
| `api_` | API 测试表 | api_project, api_request |
| `ui_` | UI 自动化表 | ui_project, ui_element |
| `app_` | APP 自动化表 | app_project, app_device |
| `req_` | 需求分析表 | req_document, req_analysis |

---

## 五、服务端必存逻辑

### 5.1 认证授权逻辑

#### 5.1.1 双 Token 机制

```
┌─────────────────────────────────────────────────────────────────────┐
│                         认证流程                                     │
└─────────────────────────────────────────────────────────────────────┘

登录请求
    │
    ▼
┌──────────────┐
│ 验证用户名   │
│ 密码         │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│ 生成双 Token                                 │
│ - Access Token: 有效期 15 分钟，存储用户信息  │
│ - Refresh Token: 有效期 7 天，存储到数据库   │
└──────┬──────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│ 返回给前端                                    │
│ { access, refresh, user }                    │
└──────────────────────────────────────────────┘

─────────────────────────────────────────────────────────────

Token 刷新流程
    │
    ▼
┌──────────────┐
│ Access Token │
│ 过期?        │
└──────┬───────┘
       │
   是   │ 否
   ▼    ▼
┌──────┐ ┌────────────┐
│ 验证  │ │ 继续使用   │
│Refresh│ │            │
│ Token │ │            │
└──┬───┘ └────────────┘
   │
   ▼
┌─────────────────────────┐
│ 检查 Refresh Token       │
│ 是否在黑名单?            │
└──────┬──────────────────┘
       │
   是   │ 否
   ▼    ▼
┌──────┐ ┌────────────┐
│ 拒绝  │ │ 生成新     │
│       │ │ Access     │
│       │ │ Token      │
└──────┘ └────────────┘

─────────────────────────────────────────────────────────────

退出登录流程
    │
    ▼
┌──────────────┐
│ 获取 Refresh │
│ Token        │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────┐
│ 将 Refresh Token 加入黑名单      │
│ (存储到 sys_token_blacklist 表) │
└─────────────────────────────────┘
```

#### 5.1.2 项目级权限控制

```java
// 权限枚举
public enum ProjectRole {
    OWNER("owner", "负责人"),
    ADMIN("admin", "管理员"),
    DEVELOPER("developer", "开发者"),
    TESTER("tester", "测试者"),
    VIEWER("viewer", "观察者");

    // 权限映射
    public static Map<String, Set<String>> PERMISSION_MAP = Map.of(
        "owner", Set.of("project:edit", "project:delete", "member:add", "member:remove", 
                        "case:create", "case:edit", "case:delete", "case:execute", "case:view"),
        "admin", Set.of("project:edit", "member:add", "member:remove",
                        "case:create", "case:edit", "case:delete", "case:execute", "case:view"),
        "developer", Set.of("case:create", "case:edit", "case:execute", "case:view"),
        "tester", Set.of("case:execute", "case:view"),
        "viewer", Set.of("case:view")
    );
}

// 权限校验服务
@Service
@RequiredArgsConstructor
public class ProjectPermissionService {
    
    public boolean hasPermission(Long projectId, Long userId, String permission) {
        // 1. 检查是否是项目成员
        // 2. 获取用户角色
        // 3. 检查角色是否有该权限
        ProjectMember member = projectMemberMapper.selectByProjectAndUser(projectId, userId);
        if (member == null) return false;
        
        Set<String> permissions = ProjectRole.PERMISSION_MAP.get(member.getRole());
        return permissions != null && permissions.contains(permission);
    }
}

// 权限校验注解
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RequireProjectPermission {
    String permission();
    String paramName() default "projectId";
}

// 权限校验切面
@Aspect
@Component
@RequiredArgsConstructor
public class PermissionAspect {
    
    @Around("@annotation(requireProjectPermission)")
    public Object checkPermission(ProceedingJoinPoint joinPoint, 
                                  RequireProjectPermission requireProjectPermission) {
        // 1. 获取项目ID
        // 2. 获取当前用户ID
        // 3. 调用权限校验服务
        // 4. 无权限则抛出异常
    }
}
```

### 5.2 统一响应结构

```java
// 统一响应包装
@Data
public class Result<T> {
    private int code;           // 业务状态码
    private String message;     // 消息
    private T data;            // 数据
    private long timestamp;     // 时间戳
    
    public static <T> Result<T> success(T data) {
        return Result.<T>builder()
            .code(200)
            .message("success")
            .data(data)
            .timestamp(System.currentTimeMillis())
            .build();
    }
    
    public static <T> Result<T> error(ResultCode code) {
        return Result.<T>builder()
            .code(code.getCode())
            .message(code.getMessage())
            .timestamp(System.currentTimeMillis())
            .build();
    }
}

// 分页响应
@Data
public class PageResult<T> {
    private List<T> records;    // 数据列表
    private long total;         // 总记录数
    private long current;        // 当前页
    private long size;          // 每页大小
    
    public static <T> PageResult<T> of(Page<T> page) {
        return PageResult.<T>builder()
            .records(page.getRecords())
            .total(page.getTotal())
            .current(page.getCurrent())
            .size(page.getSize())
            .build();
    }
}

// 响应码枚举
public enum ResultCode {
    SUCCESS(200, "操作成功"),
    
    // 认证相关 1000-1099
    USERNAME_PASSWORD_ERROR(1001, "用户名或密码错误"),
    TOKEN_EXPIRED(1002, "Token 已过期"),
    TOKEN_INVALID(1003, "Token 无效"),
    REFRESH_TOKEN_EXPIRED(1004, "Refresh Token 已过期"),
    
    // 权限相关 1100-1199
    NO_PERMISSION(1101, "无权限操作"),
    
    // 业务相关 2000-2999
    RESOURCE_NOT_FOUND(2001, "资源不存在"),
    PROJECT_NOT_FOUND(2002, "项目不存在"),
    
    // 系统相关 5000-5999
    SYSTEM_ERROR(5001, "系统错误"),
    SERVICE_UNAVAILABLE(5002, "服务不可用");
    
    private final int code;
    private final String message;
}
```

### 5.3 异常处理逻辑

```java
// 业务异常
public class BusinessException extends RuntimeException {
    private final int code;
    
    public BusinessException(ResultCode resultCode) {
        super(resultCode.getMessage());
        this.code = resultCode.getCode();
    }
    
    public BusinessException(int code, String message) {
        super(message);
        this.code = code;
    }
}

// 全局异常处理器
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusinessException(BusinessException e) {
        return Result.error(e.getCode(), e.getMessage());
    }
    
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValidationException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
            .map(error -> error.getField() + ": " + error.getDefaultMessage())
            .collect(Collectors.joining(", "));
        return Result.error(400, message);
    }
    
    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error(ResultCode.SYSTEM_ERROR);
    }
}
```

### 5.4 定时任务逻辑 (XXL-JOB)

```java
// XXL-JOB 任务注册
@Component
@RequiredArgsConstructor
public class ApiScheduledTaskJob {
    
    private final ApiScheduledTaskService taskService;
    private final ApiTestExecutor apiTestExecutor;
    
    // API 定时任务执行
    @XxlJob("apiScheduledTaskJob")
    public ReturnT<String> execute(String param) {
        // 1. 解析参数，获取任务ID
        // 2. 查询任务配置
        // 3. 执行 API 测试
        // 4. 保存执行结果
        // 5. 发送通知（如果配置）
        return ReturnT.SUCCESS;
    }
}

// 任务执行状态
public enum TaskStatus {
    PENDING("pending", "待执行"),
    RUNNING("running", "执行中"),
    SUCCESS("success", "执行成功"),
    FAILED("failed", "执行失败");
    
    private final String value;
    private final String description;
}
```

### 5.5 文件上传逻辑

```java
// 文件存储策略
public interface FileStorageStrategy {
    String upload(MultipartFile file, String path);
    void delete(String path);
    String getUrl(String path);
}

// 本地存储实现
@Component
@RequiredArgsConstructor
public class LocalFileStorageStrategy implements FileStorageStrategy {
    
    private final AppConfig appConfig;
    
    @Override
    public String upload(MultipartFile file, String path) {
        String fullPath = appConfig.getUploadPath() + "/" + path;
        // 保存文件到本地
        return "/media/" + path;
    }
}

// MinIO 存储实现
@Component
@RequiredArgsConstructor
public class MinioFileStorageStrategy implements FileStorageStrategy {
    
    @Override
    public String upload(MultipartFile file, String path) {
        // 上传到 MinIO
        // 返回访问URL
        return minioClient.getPresignedObjectUrl(bucket, path);
    }
}

// 文件上传服务
@Service
@RequiredArgsConstructor
public class FileUploadService {
    
    private final FileStorageStrategy storageStrategy;
    
    public String uploadFile(MultipartFile file, String directory) {
        // 1. 校验文件大小、类型
        // 2. 生成唯一文件名
        // 3. 调用存储策略上传
        // 4. 返回访问路径
    }
}
```

### 5.6 审计日志逻辑

```java
// 操作日志注解
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface OperLog {
    String module() default "";
    String operation() default "";
}

// 审计日志服务
@Service
@RequiredArgsConstructor
public class AuditLogService {
    
    public void log(Long userId, String module, String operation, 
                    String targetType, Long targetId, String detail) {
        SysAuditLog log = SysAuditLog.builder()
            .userId(userId)
            .module(module)
            .operation(operation)
            .targetType(targetType)
            .targetId(targetId)
            .detail(detail)
            .ipAddress(getClientIp())
            .createTime(LocalDateTime.now())
            .build();
        auditLogMapper.insert(log);
    }
}

// 审计日志切面
@Aspect
@Component
@RequiredArgsConstructor
public class OperLogAspect {
    
    @Around("@annotation(operLog)")
    public Object logOperation(ProceedingJoinPoint joinPoint, OperLog operLog) {
        // 执行前记录
        // 执行方法
        // 执行后记录
    }
}
```

---

## 六、架构迭代记录

### 6.1 迭代日志

| 日期 | 版本 | 修改内容 | 修改原因 |
|------|------|----------|----------|
| 2026-04-13 | v1.0 | 初稿：目录分层、核心模块、数据模型、服务端必存逻辑 | 初始架构设计 |

### 6.2 待讨论项

| 项 | 说明 | 状态 |
|----|------|------|
| 事务边界 | Service 层事务边界如何划分 | 待定 |
| 缓存策略 | 哪些数据需要 Redis 缓存 | 待定 |
| 日志规范 | 结构化日志还是普通文本日志 | 待定 |
| API 版本 | 是否需要 /api/v1/ 前缀 | 待定 |

---

## 七、附录

### 7.1 技术栈对应

| 技术 | 在架构中的位置 |
|------|----------------|
| Spring Boot 3.2 | 基础框架 |
| MyBatis-Plus 3.5 | ORM、代码生成 |
| Spring Security | 认证授权 |
| JJWT 0.12 | Token 管理 |
| Redis 7 | Token 黑名单、缓存 |
| XXL-JOB 2.4 | 定时任务 |
| MinIO | 文件存储 |
| LangChain4j | AI 模型调用 |
| SpringDoc OpenAPI | API 文档 |

### 7.2 参考资料

- [Spring Boot 架构设计最佳实践](https://spring.io/projects/spring-boot)
- [MyBatis-Plus 代码生成器](https://baomidou.com/pages/981404/)
- [XXL-JOB 任务调度](https://www.xuxueli.com/xxl-job/)
