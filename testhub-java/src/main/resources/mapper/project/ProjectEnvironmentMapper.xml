<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.testhub.modules.ai_generation.mapper.ProjectEnvironmentMapper">

    <resultMap id="BaseResultMap" type="com.testhub.modules.ai_generation.domain.ProjectEnvironment">
        <id column="id" property="id"/>
        <result column="project_id" property="projectId"/>
        <result column="name" property="name"/>
        <result column="base_url" property="baseUrl"/>
        <result column="description" property="description"/>
        <result column="variables" property="variables"/>
        <result column="is_default" property="isDefault"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByProjectId" resultMap="BaseResultMap">
        SELECT * FROM prj_project_environment
        WHERE project_id = #{projectId}
        ORDER BY is_default DESC, created_at ASC
    </select>

    <select id="selectDefaultByProjectId" resultMap="BaseResultMap">
        SELECT * FROM prj_project_environment
        WHERE project_id = #{projectId} AND is_default = true
        LIMIT 1
    </select>

</mapper>
